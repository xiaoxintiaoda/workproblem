今天把做公司OA后台，有一个接口需要线上调试，于是把代码传到服务器运行，git clone代码、composer install代码，配置nginx vhost文件后，设置的域名没生效，因为这台服务器上早就设置过ip,于是是新增的一个端口域名， 最后试了很久后发现，因为前端有负债均衡的机器做代理，所以我在当前机器上设置vhost是无效的，域名指向的ip并不是当前这个机器，所以只能使用ip+端口的方式访问服务。以前也碰到过这种问题，但是没有记录，今天又犯了一次，花了些时间调试。此后再碰到这种问题，第一步就应该是查看域名指向的ip是多少。
